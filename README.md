# FILE-INVENTORY-SYSTEM
This Python program manages student records using a CSV file. It allows users to add student details (ID, name, marks), calculates total, average, and grade, and stores them. Users can also view all saved records through a simple menu-driven interface.
